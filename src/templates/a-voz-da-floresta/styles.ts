import styled from 'styled-components'

export const Wrapper = styled.section`
  position: relative;
  background-color: #fff;
  min-height: 100vh;
  overflow: hidden;

  .react-reveal {
    z-index: 100;
  }
`
